# Command-line parser examples

*argparse_subparser.py* - An example that shows how to use the Argument Parser module with sub commands

*argparse_subparser_expand.py* - An example very similar to the one above that also shows how to have the argparse call a custom command on the subcommand with normal argument parameters

# Web server examples

* simple_web.py * - A simple web server example that

* template * - A folder that shows an example of a webserver that uses the Mako templating library to render the text of a template.
